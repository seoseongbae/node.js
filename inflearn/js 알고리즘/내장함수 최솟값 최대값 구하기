내장함수에 배열만 넣으면 인자로만 넘어가기때문에 값을 구해주지 않는다 그래서,
... = 전개연산자를 사용하면 내장함수를 사용할때 배열값들을 펼쳐서 값을 구해준다.

<html>
    <head>
        <meta charset="UTF-8">
        <title>출력결과</title>
    </head>
    <body>
        <script>
            function solution(arr) {
                let answer=Math.min(...arr);
                console.log(Math.max(...arr));
                return answer;
            }
            let arr=[5,7,1,3,2,9,11];
           console.log(solution(arr));
           
        </script>
    </body>
</html>


